import XCTest
@testable import TaskManagerAppKit

final class TaskManagerAppKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TaskManagerAppKit().text, "Hello, World!")
    }
}
